---
# defaults file for filebeat-role
filebeat_version: "7.14.0"
filebeat_install_type: remote
elastic_service_ip: "{{ hostvars['elastic-host']['ansible_facts']['default_ipv4']['address'] }}"
kibana_service_ip: "{{ hostvars['kibana-host']['ansible_facts']['default_ipv4']['address'] }}"
